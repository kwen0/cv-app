import React from "react";

class Header extends React.Component {
    render() {
        return <div>
            <h1>Welcome!</h1>
            <h2>Enter the details below in order to generate your CV.</h2>
        </div>
    }
}

export default Header;